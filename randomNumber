var number = [0, 1, 2, 3, 4, 5, 6];
function randomNumber(number) {
    var numberList = number.length;
    var randomNumber1 = Math.floor(Math.random() * numberList);
    var randomNumbers = number[randomNumber1];

    return randomNumbers;

}

randomNumber();